# 🤖 Инструкция по использованию исправленного бота

## 📦 Что было исправлено

✅ **Критическая ошибка после ЕГЭ Setup (шаг 3/3)** — теперь работает  
✅ **Ошибки при потере session данных** — добавлены проверки  
✅ **Потеря данных БД** — добавлены явные коммиты  
✅ **Зависания при медленном интернете** — улучшены таймауты  
✅ **Непонятные ошибки юзерам** — добавлены информативные сообщения  

---

## 🚀 Быстрый старт

### 1. Убедись что все файлы на месте:
```
mood_bot/
├── bot.py               ✅
├── database.py          ✅ (исправлен)
├── ege.py              ✅ (исправлен)
├── keyboards.py        ✅
├── morning.py          ✅
├── evening.py          ✅
├── planner.py          ✅ (исправлен)
├── report.py           ✅
├── ai_coach.py         ✅ (исправлен)
├── Procfile            ✅
├── requirements.txt    ✅
├── FIXES_LOG.md        📝 (новый файл — лог исправлений)
└── USAGE.md            📝 (этот файл)
```

### 2. Запусти бота:
```bash
python bot.py
```

### 3. Тестируй основные функции:

#### 📚 ЕГЭ Setup (самое важное — это было сломано!)
```
/ege_setup
→ Выбери предметы (✅ кнопки каждого)
→ Выбери дни недели
→ Готово → ПРОВЕРЬ: сообщение о сохранении должно появиться!
```

#### 🌅 Утро
```
🌅 Утренний опрос
→ Выбери часы сна
→ Выбери минуты
→ Оцени качество
→ Были сны?
→ Телефон?
→ Должно сохраниться в БД
```

#### 🌙 Вечер
```
🌙 Вечерний дневник
→ Заполни все вопросы
→ Добавь заметку или /skip
→ Проверь сохранение
```

#### 📋 План на день
```
📋 План на день
→ Выбери предметы
→ Для каждого выбери тип задачи
→ Добавь свою задачу с напоминанием
→ Готово → проверь сохранение
```

#### 📊 Отчёты
```
📊 Мой день    → показывает сегодняшний прогресс
📈 Отчёт недели → недельная статистика
🤖 AI-тренер    → советы на неделю
```

---

## 🐛 Если что-то сломалось

### Ошибка: "⚠️ Ошибка: не удалось сохранить пользователя"
- **Причина:** БД недоступна или пермиссии файла
- **Решение:** 
  ```bash
  rm diary.db  # Удали старую БД
  python bot.py  # Бот пересоздаст
  ```

### Ошибка: "⚠️ Ошибка: пользователь не найден"
- **Причина:** Нажали какую-то кнопку не по порядку
- **Решение:** Нажми `/start` и начни заново

### Ошибка: "⚠️ Неизвестный предмет" в ЕГЭ
- **Причина:** Выбран неправильный предмет (это не должно происходить)
- **Решение:** Проверь /ege_setup, переделай настройки

### Таймаут при AI-тренере (ждёт 60 сек)
- **Причина:** Медленный интернет или API Anthropic недоступен
- **Решение:** Попробуй позже, или используй fallback советы (они генерируются без API)

---

## 📝 Логирование

### Включи логирование для отладки:
```python
# В bot.py уже включено:
logging.basicConfig(
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    level=logging.INFO
)
```

### Проверь логи:
```bash
# Linux/Mac:
tail -f output.log | grep ERROR

# Windows (PowerShell):
Get-Content output.log -Tail 50 -Wait
```

---

## 🔒 Безопасность

### Переменные окружения:
Убедись что в Railway/Heroku/Replit установлены:
```
TOKEN=<твой_bot_token>
```

**НИКОГДА** не коммитить TOKEN в код!

---

## 📊 Мониторинг БД

### Проверить текущее состояние БД:
```bash
# Установи sqlite3:
pip install sqlite3

# Просмотри таблицы:
sqlite3 diary.db
.tables
SELECT * FROM users;
SELECT * FROM ege_settings;
```

### Очистить БД (если нужно стартовать с нуля):
```bash
rm diary.db  # БД пересоздастся автоматически
```

---

## 🎯 Основные потоки использования

### День новичка:
```
1. /start              → регистрация
2. /ege_setup          → настройка ЕГЭ
3. 🌅 Утренний опрос   → заполни
4. 📋 План на день     → планируй
5. ✅ Отметить         → отмечай выполнение
6. 🌙 Вечерний дневник → рефлексия
```

### Типичный день:
```
07:30 → 🌅 Напоминание утром (автоматически)
        Заполни утренний опрос
09:00 → 📚 ЕГЭ — добавь занятие
13:00 → 💧 Напоминание пить воду (автоматически)
14:00 → 📋 План на день — проверь прогресс
16:00 → 📵 Напоминание про фокус (автоматически)
22:00 → 🌙 Напоминание вечером (автоматически)
        Заполни дневник
```

### Воскресенье:
```
20:00 → 📈 Недельный отчёт (автоматически)
        + 🤖 AI-тренер анализирует неделю
```

---

## ❓ FAQ

**Q: Бот не отправляет напоминания в 7:30**  
A: Время в коде указано в UTC. Твоё время = UTC + часовой_пояс. Проверь `bot.py` строка с `time=time(2, 30)` — это 7:30 по Перми (UTC+5).

**Q: Зачем выполнять задачи в "Отметить" если они автосохраняются?**  
A: Ты можешь переделать их позже. "Отметить" — это подтверждение готовности, и это важно для статистики.

**Q: Почему AI-тренер иногда не отвечает?**  
A: API может быть недоступен. Используются fallback советы — они основаны на стобалльниках и тоже полезны.

**Q: Как экспортировать данные?**  
A: Данные хранятся в `diary.db`. Используй:
```bash
sqlite3 diary.db ".mode csv" "SELECT * FROM entries;" > export.csv
```

---

## 🔄 Обновление в будущем

Если нужны новые фичи:
1. Сделай бэкап: `cp diary.db diary.db.backup`
2. Обновляй код
3. Протестируй локально
4. Если ошибка — восстанови из бэкапа

---

## 📞 Контакт для ошибок

Если нашёл баг:
1. Запусти с `level=logging.DEBUG` для отладки
2. Скопируй ошибку из логов
3. Проверь [FIXES_LOG.md](./FIXES_LOG.md) — может уже исправлено

---

**Версия:** 1.0  
**Последнее обновление:** 26.04.2026  
**Статус:** ✅ Готово к использованию
